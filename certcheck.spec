# -*- mode: python ; coding: utf-8 -*-
# PyInstaller spec — builds a standalone, single-file certcheck.exe (no Python
# needed at runtime). Build with:  pyinstaller certcheck.spec   (or .\build.ps1)

a = Analysis(
    ['certcheck.py'],
    pathex=[],
    binaries=[],
    datas=[],
    hiddenimports=[],
    hookspath=[],
    runtime_hooks=[],
    excludes=[],
    noarchive=False,
)
pyz = PYZ(a.pure)

exe = EXE(
    pyz,
    a.scripts,
    a.binaries,
    a.datas,
    [],
    name='certcheck',
    debug=False,
    bootloader_ignore_signals=False,
    strip=False,
    upx=False,
    console=True,
    disable_windowed_traceback=False,
    argv_emulation=False,
    target_arch=None,
    codesign_identity=None,
    entitlements_file=None,
)
